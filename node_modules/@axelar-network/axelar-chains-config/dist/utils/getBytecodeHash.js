var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var keccak256 = require('@ethersproject/keccak256').keccak256;
/**
 * Compute bytecode hash for a deployed contract or contract factory as it would appear on-chain.
 * Some chains don't use keccak256 for their state representation, which is taken into account by this function.
 * @param {Object} contractObject - An instance of the contract or a contract factory (ethers.js Contract or ContractFactory object)
 * @returns {Promise<string>} - The keccak256 hash of the contract bytecode
 */
function getBytecodeHash(contractObject, chain, provider) {
    if (chain === void 0) { chain = ''; }
    if (provider === void 0) { provider = null; }
    return __awaiter(this, void 0, void 0, function () {
        var bytecode;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    if (!isString(contractObject)) return [3 /*break*/, 2];
                    if (provider === null) {
                        throw new Error('Provider must be provided for chain');
                    }
                    return [4 /*yield*/, provider.getCode(contractObject)];
                case 1:
                    bytecode = _a.sent();
                    return [3 /*break*/, 5];
                case 2:
                    if (!contractObject.address) return [3 /*break*/, 4];
                    // Contract instance
                    provider = contractObject.provider;
                    return [4 /*yield*/, provider.getCode(contractObject.address)];
                case 3:
                    bytecode = _a.sent();
                    return [3 /*break*/, 5];
                case 4:
                    if (contractObject.bytecode) {
                        // Contract factory
                        bytecode = contractObject.bytecode;
                    }
                    else {
                        throw new Error('Invalid contract object. Expected ethers.js Contract or ContractFactory.');
                    }
                    _a.label = 5;
                case 5:
                    if (chain.toLowerCase() === 'polygon-zkevm') {
                        throw new Error("polygon-zkevm is not supported. Use getBytecodeHash from axelar-contract-deployments that handles polygon-zkevm's hash function");
                    }
                    return [2 /*return*/, keccak256(bytecode)];
            }
        });
    });
}
var isString = function (arg) {
    return typeof arg === 'string' && arg !== '';
};
module.exports = {
    getBytecodeHash: getBytecodeHash,
};
