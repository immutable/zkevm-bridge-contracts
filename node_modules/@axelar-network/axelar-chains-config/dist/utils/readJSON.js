var readJsonSync = require('fs-extra').readJsonSync;
var readJSON = function (filePath, require) {
    if (require === void 0) { require = false; }
    var data;
    try {
        data = readJsonSync(filePath, 'utf8');
    }
    catch (err) {
        if (err.code === 'ENOENT' && !require) {
            return undefined;
        }
        throw err;
    }
    return data;
};
module.exports = {
    readJSON: readJSON,
};
