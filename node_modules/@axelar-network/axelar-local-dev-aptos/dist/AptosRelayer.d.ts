import { RelayCommand, Command, Relayer, RelayerType, CallContractArgs, RelayData } from '@axelar-network/axelar-local-dev';
export declare class AptosRelayer extends Relayer {
    constructor();
    setRelayer(type: RelayerType, _: Relayer): void;
    updateEvents(): Promise<void>;
    execute(commands: RelayCommand): Promise<void>;
    executeAptosToEvm(commandList: RelayCommand): Promise<void>;
    private executeEvmToAptos;
    private executeAptosGateway;
    private executeAptosExecutable;
    private executeEvmGateway;
    private executeEvmExecutable;
    private updateGasEvents;
    private updateCallContractEvents;
    createCallContractCommand(commandId: string, relayData: RelayData, contractCallArgs: CallContractArgs): Command;
    createCallContractWithTokenCommand(): Command;
}
//# sourceMappingURL=AptosRelayer.d.ts.map